{
  "name": "@writtendev/ui",
  "version": "0.0.0",
  "private": true,
  "description": "Shared component package for written's web client. Contents land in WRTN-42; this package establishes the workspace shape it lands into. No build script: it has no publish step and is consumed directly from src/ by web's bundler (see ARCHITECTURE.md).",
  "buildless": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
