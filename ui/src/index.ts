// Placeholder entry point for the shared component package.
//
// This package is intentionally empty. Its real contents (components, theme
// tokens, shared types) land starting with WRTN-36; this file exists only so
// the package resolves as a workspace dependency of `web` and `tsc --noEmit`
// has something to check.
export const UI_PACKAGE_PLACEHOLDER = true
