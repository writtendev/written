# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
/written

# Test binary, built with `go test -c`
*.test

# Code coverage profiles and other test artifacts
*.out
coverage.*
*.coverprofile
profile.cov

# Dependency directories
# vendor/
node_modules/

# Go workspace file
go.work
go.work.sum

# env file
.env
*.local

# npm logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Editor/IDE
.idea
.vscode/*
!.vscode/extensions.json
.DS_Store
*.sw?

# lerp's run records, workspaces and logs
.lerp/

# VHS demo/cast scratch directory
.demo/

# goreleaser snapshot/release output
dist/

# Scratch & temporary files
tmp/
.agents/skills/
.claude/worktrees/
.claude/settings.local.json

# Symlinks to the shared agent skills in the parent studio repo. They
# point outside this repo, and this repo is public, so they must never
# be committed — same reasoning as the worktrees above.
.claude/skills/
