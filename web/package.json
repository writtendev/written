{
  "name": "@writtendev/web",
  "version": "0.0.0",
  "private": true,
  "description": "Embedded Vite client for `written web`. Built at compile time and served from the Go binary; the actual UI is out of scope for this ticket and lands once @writtendev/ui has real contents (WRTN-42).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@writtendev/ui": "*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^8.2.2"
  }
}
