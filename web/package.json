{
  "name": "@writtendev/web",
  "version": "0.0.0",
  "private": true,
  "description": "Embedded Vite client for `written web`. Built at compile time and served from the Go binary; the actual client UI lands once @writtendev/ui has real contents (WRTN-36 onward).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@writtendev/ui": "*"
  },
  "devDependencies": {
    "@types/react": "^19.2.18",
    "typescript": "~6.0.2",
    "vite": "^8.2.2"
  }
}
